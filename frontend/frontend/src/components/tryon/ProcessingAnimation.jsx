import { ArrowDown, Sparkles } from 'lucide-react'

function ProcessingAnimation({ photo, product, progress = 0 }) {
  const price = product && typeof product.price === 'number' ? `₹${product.price.toLocaleString('en-IN')}` : product?.price
  return <div className="rounded-lg border border-line bg-surface p-5 shadow-[var(--shadow-soft)] sm:p-8"><div className="grid items-center gap-4 sm:grid-cols-[1fr_auto_1fr]"><div className="overflow-hidden rounded-md border border-line bg-canvas"><div className="aspect-[4/3]">{photo?.previewUrl ? <img src={photo.previewUrl} alt="Your uploaded photo" className="h-full w-full object-contain" /> : <div className="flex h-full items-center justify-center text-sm text-muted">Your photo</div>}</div><p className="border-t border-line px-3 py-2 text-xs font-bold uppercase tracking-[0.12em] text-muted">Your photo</p></div><div className="flex items-center justify-center text-accent"><span className="hidden size-10 items-center justify-center rounded-full bg-accent-soft sm:inline-flex"><Sparkles size={18} className="animate-pulse" aria-hidden="true" /></span><ArrowDown size={18} className="sm:hidden" aria-hidden="true" /></div><div className={`overflow-hidden rounded-md border border-line bg-canvas ${product?.visualClass || 'bg-accent-soft'}`}><div className="aspect-[4/3]">{product?.image ? <img src={product.image} alt={product.name} className="h-full w-full object-cover" /> : <div className="flex h-full items-center justify-center text-sm text-muted">Selected clothing</div>}</div><div className="border-t border-line bg-surface px-3 py-2"><p className="truncate text-xs font-bold uppercase tracking-[0.12em] text-muted">{product?.name || 'Selected clothing'}</p>{price ? <p className="mt-1 text-sm font-semibold text-ink">{price}</p> : null}</div></div></div><div className="mt-8 border-t border-line pt-6 text-center"><div className="mx-auto inline-flex items-center gap-2 text-sm font-semibold text-accent"><Sparkles size={17} className="animate-pulse" aria-hidden="true" /> Creating your personalized preview</div><div className="mt-5 h-2 overflow-hidden rounded-full bg-accent-soft" role="progressbar" aria-valuemin="0" aria-valuemax="100" aria-valuenow={progress} aria-label="Try-On processing progress"><div className="h-full rounded-full bg-accent transition-[width] duration-300" style={{ width: `${progress}%` }} /></div><div className="mt-2 flex justify-between text-xs text-muted"><span>Your personalized look is being prepared...</span><span className="font-bold text-accent">{progress}%</span></div></div></div>
}

export default ProcessingAnimation
