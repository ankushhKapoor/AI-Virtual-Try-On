import ResultImagePlaceholder from '../tryon/ResultImagePlaceholder'

function ComparisonGrid({ looks }) {
  return <section aria-label="Look comparison"><div className="mb-5"><p className="text-xs font-bold uppercase tracking-[0.14em] text-accent">Side by side</p><h2 className="mt-2 text-2xl font-semibold tracking-[-0.03em] text-ink">Your comparison</h2></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{looks.map((look) => { const product = look.product; const price = typeof product.price === 'number' ? `₹${product.price.toLocaleString('en-IN')}` : product.price; return <article key={look.id} className={`rounded-md border bg-surface p-3 ${look.favorite ? 'border-[#b8893f] ring-1 ring-[#b8893f]' : 'border-line'}`}><ResultImagePlaceholder resultImage={look.resultImage} className="aspect-[3/4]" /><div className="space-y-2 p-2"><div className="flex items-start justify-between gap-2"><h3 className="font-semibold text-ink">{product.name}</h3>{look.favorite ? <span className="text-[#9a6b25]" aria-label="Favorite look">★</span> : null}</div><dl className="space-y-1 text-sm text-muted"><div className="flex justify-between gap-2"><dt>Category</dt><dd className="font-medium text-ink">{product.category}</dd></div><div className="flex justify-between gap-2"><dt>Price</dt><dd className="font-medium text-ink">{price}</dd></div><div className="flex justify-between gap-2"><dt>Rating</dt><dd className="font-medium text-ink">★ {product.rating}</dd></div><div className="flex justify-between gap-2"><dt>Color</dt><dd className="font-medium text-ink">{product.color}</dd></div>{product.selectedSize ? <div className="flex justify-between gap-2"><dt>Size</dt><dd className="font-medium text-ink">{product.selectedSize}</dd></div> : null}</dl><p className={`pt-2 text-xs font-semibold ${look.saved ? 'text-success' : 'text-muted'}`}>{look.saved ? 'Saved' : 'Not saved'}</p></div></article> })}</div></section>
}

export default ComparisonGrid
